name: Call a reusable workflow

on:
  workflow_dispatch:

jobs:
  reusable-job:
    uses: stevenarnold-ed-fi/ed-fi-actions/.github/workflows/reusable.yml@main
    with:
      username: ./github/workflows/config.json
    secrets:
      token: ./bidi-scanner/config.json123