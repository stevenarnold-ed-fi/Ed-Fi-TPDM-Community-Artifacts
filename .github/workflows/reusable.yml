name: Call a reusable workflow

on:
  workflow_dispatch:

jobs:
  reusable-job:
    uses: stevenarnold-ed-fi/ed-fi-actions/.github/workflows/reusable.yml@latest
    with:
      config-file-path: ./.github/workflows/bidi-scanner/config.json
